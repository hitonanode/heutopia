num_process: 4
num_case_limit: 512
scoreboard_testcases: 150
dataset_dir: "sample_input"

print_columns:
  - score

runner:
  run_comand: "cat {INPUT_FILE} | {SOLVER_CMD} 2> {SOLVER_OUTPUT}"

# optuna:
#   num_trials: 20

# Google Sheets に出力したい場合
# google_sheets:
#   gcp_sa_path: "path/to/gcp_credential.json"
#   sheet_key: "***"
